# Dashboard

Dashboard lokal tersedia pada:

```text
http://localhost:3000/dashboard
```

Dashboard menampilkan:

- readiness;
- status live protocol;
- capability summary;
- daftar session.

API key disimpan hanya di `sessionStorage` browser. Dashboard tidak memuat CDN atau asset eksternal.
