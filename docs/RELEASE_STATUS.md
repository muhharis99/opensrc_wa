# Release Status

## v0.2.0

- Publication status: published to `main`.
- Runtime scope: broad clean-room mock and fixture feature parity.
- Local validation: lint, format check, strict TypeScript type-check, build, documentation checks, dependency policy checks, license checks, and 30 automated tests passed.
- Gateway smoke test: `/health`, `/ready`, `/dashboard`, and `/api/v1/capabilities` passed; the capability registry exposes 41 records.
- Live WhatsApp protocol: `BLOCKED` until lawful clean-room research and live testing with owned test accounts provide sufficient evidence.
- Forbidden gateway dependencies, wrappers, forks, copied source, and browser automation are not used.
